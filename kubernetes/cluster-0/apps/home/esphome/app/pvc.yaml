---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esphome
  namespace: home
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-filesystem