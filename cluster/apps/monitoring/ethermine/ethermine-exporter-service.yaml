apiVersion: v1
kind: Service
metadata:
  name: ethermine-exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: ethermine-exporter
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: ethermine-exporter
