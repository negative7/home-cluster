---
apiVersion: v1
kind: Service
metadata:
  name: gminer-exporter
  namespace: monitoring
  labels:
    app: gminer-exporter
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    app: gminer-exporter
